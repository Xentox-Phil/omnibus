[project]
name = "omnibus-backend"
version = "0.1.0"
description = "FastAPI backend for Omnibus — serves the demand surface + event curves"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
]

[tool.uv]
package = false

[dependency-groups]
dev = []
